# V3 学习计划改造方案(v3.1)

> 立于 2026-06-04(D6 收口后)。起因:D6 暴露实战日强度不足、教学深度不够。
> 目标:把计划从"广度覆盖"升级为"能学透、能转岗"的高强度体系。
> 节奏:**定原则(本文档) + 近期细排(D7/Phase2) + 远期滚动(Phase3-9 走到前一周再细化)**。
> 主公拍板:认可此节奏;先落原则 + 重排 D7 做样板。

---

## 一、诊断

**优点**:覆盖系统、项目驱动、里程碑清晰、边投边学务实——骨架是好的,不推倒。

**风险**(D6 暴露 + 通盘审视):
1. **颗粒度不均**:简单主题(CLI)8h 偏大;难主题(Embedding/LangGraph/RAG 调优)2h 偏小。
2. **深度 vs 广度**:38 周铺太广,深度恐不到"企业级"面试/实战要求。
3. **基础内功缺口**:OS/网络/内存/并发未系统补,而企业级岗位面试必深挖系统设计与原理。
4. **项目区分度**:偏功能堆叠,需加真实复杂度 + 量化成果,简历才打得动。
5. **助教执行**:此前教学"够用即止",强度/深度/资料未拉满(已纠正,本方案约束)。

**转岗判断(不粉饰)**:计划是**必要非充分条件**。严格执行能给扎实广度、达中级 AI 应用工程师知识面;但"企业级全栈"高标准,靠 38 周自学深度有风险,现实路径更可能是"先转中级 AI 应用岗 → 工作中长到企业级"。决定成败的不是覆盖面(已够),是**执行深度 + 内功 + 项目区分度 + 面试**。主公的多年工程经验是真优势,方向没错。

---

## 二、改造原则(五条,全程适用)

| # | 原则 | 改造动作 |
|:--|:--|:--|
| 1 | **强度** | 实战日 = 真项目 + 扩展挑战 + 深挖(见 §三模板) |
| 2 | **深度** | L2 七段讲到位 + 每点配资料 + 关键机制下探源码/原理 |
| 3 | **内功** | 关联式补 OS/网络/内存/并发(见 §四) |
| 4 | **颗粒度** | 按难度重排 timebox:简单的压缩、难的扩展或拆分 |
| 5 | **项目区分度** | 真实复杂度 + 量化成果(QPS/延迟/覆盖率/成本),简历能打 |

---

## 三、休息日 / 实战日新模板(8h,宽而深)

休息日 / 实战日不是把工作日 2h 内容简单拉长，而是用 8h 做一个可验证的真实工程闭环：核心开发要有产出，知识要讲透，资料要能查，面试表达要能说。

```
├─ 核心实现(3h)    主线项目,核心开发亲手写
├─ 扩展挑战(2h)    真实工程特性:重试退避/限流/配置化/测试/日志/可观测
├─ 深度追问(1.5h)  L2 深挖 + 源码/原理下探 + Codex 连环追问
├─ 内功关联(1h)    本主题相关的 OS/网络/内存/并发原理就地补
└─ 资料精读(0.5h)  官方文档关键章节 + 复盘
```

### 休息日 / 实战日执行颗粒度:三线合一

| 线 | 目标 | 8h 当天必须体现 |
|:--|:--|:--|
| 开发线 | 能真正写业务/工程代码 | 核心实现 + 扩展挑战形成可运行产出,有明确输入输出、错误处理、边界和验收方式 |
| 知识线 | 能理解底层和迁移 | 概念地图 + 1 个主线主题 + 2-3 个核心机制 L2 深挖 + 4-8 个支撑概念串联 + 旧栈/内功对照 |
| 面试线 | 能说清并经得起追问 | 收口时转成 3-5 个面试问法、回答骨架和追问点 |

### 概念颗粒度:一主线多核心

- 1 个主线主题:当天工程任务围绕一个真实场景闭环,避免散成多个无关知识点。
- 2-3 个核心机制按 L2 标准深挖:问题起源、底层机制、设计动机、边界陷阱、近义辨析、旧栈对照、递进示例。
- 4-8 个支撑概念讲清:是什么、为什么、怎么用、常见坑、与核心机制的关系。
- 若干术语钩子首次出现时就地解释;若当天来不及深挖,进入滚动清单,不得消失。
- 8h 的更多时间优先用于主线开发、工程扩展和机制验证,不是增加大段文档阅读。

### 资料包与面试转换

- 每个休息日 / 实战日必须给资料包:必读、查阅、延伸、阅读后回答 2-3 问。
- 资料精读固定服务 0.5h 时间盒;文档用于支撑开发、深挖和面试表达,不把实战日变成读文档日。
- 资料优先级:官方文档 / 经典文章 / 源码或规范 / 高质量工程案例。
- 面试转换必须在收口前完成:当天主题至少产出 3-5 个面试问法,并标注可继续追问的点。

### 8h 刹车规则

- 前 3h 优先保证主线开发产出,不被资料、测试样板或支线问题抢走。
- 5h 后若核心实现和扩展挑战仍未收束,停止追加新功能,优先完成可验证闭环。
- 6.5h 后不再开启新的大坑,只做深度追问、资料精读、验收和台账整理。
- 8h 到点必须收口:区分"工程已达标"、"深挖滚动未完成"、"资料未读完",允许滚动顺延,不得靠硬拖补清单。

### 休息日 / 实战日验收标准

1. 开发线:核心逻辑亲手完成,代码能跑或有明确验证证据。
2. 知识线:能复述当天主线主题、2-3 个核心机制,并能说出至少两个边界陷阱。
3. 面试线:能回答当天 3-5 个面试问法中的核心题。
4. 资料线:完成必读资料的阅读任务,或明确标记为滚动补读。
5. 若代码达标但概念或面试表达未达标,只记为"工程达标,学习未完全达标"。

### 2h 工作日模板(窄而深,三线合一)

工作日主题要窄,但不能把知识密度砍没。2h 的正确做法是"一主多辅":1 个主概念深挖,2-4 个支撑概念建立知识网,再用一个小开发任务把知识落到代码里。时间不够时滚动补深,不得跳过资料查阅和面试转换。

```
├─ 概念地图(0.15h)    主概念 + 2-4 个支撑概念 + 术语钩子,先看全貌
├─ 主概念深挖(0.35h)  L2:问题起源 / 底层机制 / 设计取舍 / 边界陷阱
├─ 支撑概念串联(0.25h) 讲清是什么 / 为什么 / 怎么用 / 常见坑 / 与主概念关系
├─ 亲手小开发(0.45h)  真实或准真实小功能,覆盖输入输出、错误处理、边界
├─ 资料查阅(0.15h)    必读 + 查阅 + 延伸 + 阅读后回答 2-3 问
└─ 面试收口(0.15h)    3-5 个面试问法 + 回答骨架 + 薄弱点滚动
```

#### 工作日三线合一验收

| 线 | 2h 当天必须体现 |
|:--|:--|
| 开发线 | 一个亲手完成的核心开发小闭环,不是纯 demo 或纯测试样板 |
| 知识线 | 1 个主概念 L2 深挖 + 2-4 个支撑概念串联 + 至少一个边界陷阱 |
| 面试线 | 3-5 个面试问法,至少能答出核心题和一个追问方向 |
| 资料线 | 给出必读 / 查阅 / 延伸,并完成或滚动记录阅读后问题 |

#### 工作日刹车规则

- 90 分钟后不再开启新大坑;只做收束、验证、复述和滚动记录。
- 卡住超过 20 分钟时,按"提示 → 定位问题 → 局部示例 → 收束顺延"处理,不直接代写完整作业。
- 控时优先压缩低价值测试样板、重复文档、非关键扩展和过细实现支线;不得压缩核心概念、机制下探、资料查阅和面试转换。
- 若 2h 装不下全部内容,保留概念地图和面试映射,把非主概念标为"今日建立连接,后续滚动补深"。

**工作日"学透"硬标准(每天至少做到,否则视同未达标)**:
1. **不止"会调"**:核心 API/概念答得出"为什么这样设计 / 底层怎么实现 / 何时不该用"。
2. 至少一处**机制下探**到原理/源码层。
3. 至少一个**边界陷阱亲手验证**(写代码触发,看实际行为,而非听讲)。
4. 至少一处**旧栈对照或内功关联**,与已有经验打通。
5. **面试转换**:当天知识能转成 3-5 个面试问法,不是只会写当天代码。
6. **考核留痕**:能复述 + 能迁移才算过,记台账滚动。

> 总纲:**实战日"宽而深"、工作日"窄而深",共同点是「深」——任何一天都不许停在"会用 API"的表层。**

---

## 四、内功补强线(关联式为主 + 集中式为辅)

**关联式**(挂着应用学,不脱节)——主题 → 内功映射:

| 学到的主题 | 就地补的内功 |
|:--|:--|
| asyncio / 事件循环(P1) | IO 模型(阻塞/非阻塞/多路复用 epoll)、线程·进程·协程、调度 |
| httpx / LLM API(P1-2) | TCP/IP、HTTP/1.1·2、TLS 握手、DNS、连接池、Keep-Alive |
| Embedding / 向量库(P4) | 内存布局、缓存局部性、向量索引(HNSW/IVF)原理、磁盘 IO |
| FastAPI / Redis(P4,6) | 并发模型、缓存三问(穿透/击穿/雪崩)、连接池、事件循环复用 |
| 高并发网关(P6) | 锁/信号量/CAS、限流(令牌桶/滑窗)、熔断、分布式锁、CAP、一致性 |
| Docker / K8s(P7) | 网络命名空间、负载均衡、资源调度、cgroup |

**集中式**(每周 Buffer 日):算法·数据结构(面试题)、系统设计(高并发 AI 系统)专项。

---

## 五、节奏与产出

```
① 定原则      本文档(一次性,全程适用)                          ✅ 已立
② 近期细排    Phase 1 剩余(D7) + Phase 2(D15-35) 按新强度逐日重排   ◀ D7 见 §六
③ 远期框架    Phase 3-9 仅标改造原则,走到每 Phase 前一周再细化
④ 产出        本《改造方案》存档 + 增量改 v3 各 Phase 文件
```

---

## 六、D7 重排样板(新模板首个样板)

**主题**:给 D6 的 fetcher CLI 补完整测试套件 + 工程化收尾 + 真实健壮性
**产出**:`tests/test_cli.py` 等 + 重试退避 + 信号量限流 + README,覆盖率 >80%

### 核心实现(3h)
- **单元测试**:mock httpx 测 `fetch_posts`/`fetch_in_batch`/`batch_iter`(复用 D4 的 patch/parametrize/asyncio)
- **集成测试**:用 Click 的 **`CliRunner`** 测 `fetch`/`batch` 命令入口(不真跑进程、不打网络)
- **异步路径**:`pytest-asyncio` 覆盖正常 + 异常(超时/404/全失败)
- **覆盖率**:`pytest-cov` 冲 >80%,识别未覆盖分支

### 扩展挑战(2h)——真实工程特性(加强度的核心)
- **重试退避**:给 `fetch` 加**指数退避 + 抖动**(429/5xx/超时才重试,4xx 不重试),并写测试验证重试次数
- **信号量限流**:给批量抓取加 `asyncio.Semaphore`,对照 D6 的 `batch_iter` 分批——**两种限流的取舍**(分批=批间串行;信号量=全程并发但限上限)
- **退出码测试**:`CliRunner` 验证 `result.exit_code`(成功 0 / 参数错 2 / 业务失败非 0)
- **参数化边界**:空输入、全失败、重试耗尽、超时

### 深度追问(1.5h)——L2 深挖
- mock `AsyncClient` 为何要 patch 在**被使用处**(D4 patch 路径的延伸 + 异步 mock 的 `AsyncMock`)
- `CliRunner` 怎么隔离 CLI(`isolated_filesystem`、捕获 stdout/exit_code、不起子进程)
- 覆盖率的**意义与局限**:行覆盖 ≠ 路径覆盖,80% ≠ 没 bug
- `pytest-asyncio` 的 event_loop fixture 机制

### 内功关联(1h)——网络 + 软件工程
- **重试退避背后的网络原理**:为什么要**指数退避 + 抖动**——惊群(thundering herd)、网络拥塞控制、雪崩;固定间隔重试为何放大故障
- **幂等性**:重试的前提(GET 幂等可重试,非幂等需幂等键)
- **测试替身分类**:mock/stub/spy/fake/dummy 的区别(软件工程概念)

### 资料精读(0.5h)
- pytest 官方:fixtures、monkeypatch、`pytest-asyncio`
- Click 官方:Testing(CliRunner)
- 经典:exponential backoff and jitter(AWS Architecture Blog 经典文)

### 验收标准(升级版)
- [ ] 单元 + 集成两层,全绿、离线可跑、覆盖率 >80%
- [ ] 重试退避有测试佐证(重试次数/退避时长可验)
- [ ] 信号量限流落地,能口述 vs 分批的取舍
- [ ] 退出码经 `CliRunner` 验证
- [ ] README + pyproject + ruff,他人可一键安装运行

### 兼本周 Buffer 确认
台账"Buffer 日 D7 确认即划"的薄弱点一并清账:Field 语义、闭包 cell、计时装饰器默写、gather 保序、side_effect、引用复制、patch 路径(连续确认)。

---

## 七、后续待办

- **Phase 1**:D1-D6 已收口;D7 按本样板执行。
- **Phase 2(D15-35)**:D7 收口后按新强度逐日细排(LLM 客户端/Prompt 工程,深度是重点)。
- **Phase 3-9**:仅保留原则标注,各 Phase 启动前一周细化。
- **定期对标**:每个里程碑(M1/M2…)拿目标岗位真实 JD 查漏补缺。
