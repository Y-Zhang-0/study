# V3 学习进度台账

> 助教模式·亦师亦友 | 考核:口头抽查 + 代码实操(强化手写)| 评级:🟢达标 🟡待补 🔴未过
> 官方日历 D1 = 2026-06-01;主公 2026-05-30 提前开课,实际进度领先官方。
> 代码区:`C:\Users\zhangyu\Desktop\code\study\ai-study\phase1\`,按天命名(D1.py、D2.py…),按 Phase 分目录(主公 2026-06-01 重整)。

## 进度总表

| 实际日期 | D 编号 | 主题 | 产出物 | 考核评级 | 薄弱点 |
|:--|:--|:--|:--|:--|:--|
| 2026-05-30 | D1 | 类型注解 + Pydantic + f-string | ai-study/D1.py | 🟢收口(口头 3🟢2🟡;代码 5 处修订已过,实跑达标) | 见薄弱点清单 |
| 2026-05-31 | D2 | async/await + 装饰器 + 对比 Node.js | ai-study/phase1/D2.py | 🟢收口(口头 Q1-Q3 3🟢;实跑达标 顺序≈3.03/并发≈1.01;装饰器/事件循环深挖至 L2;3 处瑕疵已修) | 见薄弱点清单 |
| 2026-06-01 | D3 | 生成器 + 上下文管理器 + httpx | ai-study/phase1/D3.py | 🟡收口(代码 🟢:批改后连环雷全部自主修复,实跑达标 8 成功/3 失败隔离、真实并发计时;口头 2🟡:gather 保序、同步 with 包 await 均未掌握;"同步/异步"术语混用 ×2) | 见薄弱点清单 |

## 薄弱点滚动清单

> 考核未过(🟡/🔴)的知识点滚动记录,Buffer 日 / 每周复盘优先补强。

- **D1 概念**(2026-05-31/D2 隔日重考):① `X | None` 必填语义 → ✅ 重考过(Buffer 日 D7 再确认即划掉)② `...` vs `gt/ge`,含 `Field(0, ...)` 第一个位置参数即 default → ✅ 重考过(Buffer 日确认)
- **概念辨析(滚动)**:闭包 vs 可变默认参数陷阱 —— D3 晨考(2026-06-01):**辨析 ✅ 大进步**(两者正确区分,B"def 时求值一次、所有调用共用同一对象"答对),但闭包底层机制(cell 对象 / `__closure__`)仍答不出("具体的不清楚")→ 🟡 半过,**D4 只重考 cell/`__closure__` 机制**
- **D2 概念**:① `enumerate[raws]` 下标不报错原因 —— D3 晨考 🟡:方向对(知道方括号是注解语法)但机制错——答"程序编译不看这些",**实际是运行时真求值**:调 `__class_getitem__` 返回 `GenericAlias` 对象(证据:`list[print('x')]` 会真的打印),且 GenericAlias 可调用故易蒙混 → 未过,D4 再考 ② `asyncio.run` 嵌套 —— D3 晨考 🔴:误答"会新开一个事件循环互不干扰",**实际抛 `RuntimeError: asyncio.run() cannot be called from a running event loop`**(实跑验证)→ 未过,D4 重考(关系 Jupyter/FastAPI 场景,必须掌握)
- **D2 默写**(D3 晨考):`async_time` 三验点(`functools.wraps` / `async def wrapper` / `await func()`)全对 🟢,但**漏了计时主体**(start/end/print)——名为计时器却没计时,默写要默全 → 🟡
- **D3 概念**(收尾考核 2026-06-01):① **`asyncio.gather` 保序**——返回顺序 = 传入顺序,与完成顺序无关(实验:完成序 3,2,1 → 返回序 1,2,3);主公误归因于"惰性加载" → 🟡 D4 重考 ② **同步 `with` 可以包住含 `await` 的代码块**(实验:`with timer():` 包 `await sleep(2)` 计时 2.00s 准确);`async with` 仅当 CM **自身进出门**需要 await(如 AsyncClient 建/关连接池);主公答"不能" → 🔴 D4 重考 ③ **同步 vs 异步术语**:同步 = 阻塞线程;异步 = 挂起协程让位事件循环;**顺序执行 ≠ 同步**——当日混用两次 → D4 重考
- **学习模式(meta,长期滚动)**:**"万能答案"倾向**——D1-D2 拿"闭包"解释一切,D3 拿"惰性加载"解释一切(连用 3 次,2 次错误归因)。对策:答"为什么"前先自问"这个概念回答的是哪类问题"(惰性只回答"何时执行",不回答"顺序保证"/"能否嵌套")
- **代码规范(长期滚动)**:标识符拼写(D1 `parase`、D2 `prase`/`wapper`)—— **D3 全天零拼写错误 ✅**(默写+作业+三轮返工),继续观察至 Buffer 日
- **D1 代码规范**:① 行尾分号 ② list 直嵌 f-string 带 `[]` ③ 命名 —— D2 代码均未复犯,已内化

### D4 晨考清单(累积待考)

1. 闭包 cell/`__closure__` 机制(D3 晨考遗留)
2. `enumerate[x]` 返回 GenericAlias 一句话(D3 晨考遗留)
3. `asyncio.run` 嵌套 → RuntimeError 一句话(D3 晨考遗留)
4. 完整版计时装饰器默写(含 `perf_counter` 计时体,D3 晨考遗留)
5. `asyncio.gather` 保序特性(D3 收尾新增)
6. 同步 `with` 能否包 `await` 代码 + `async with` 的真正用途(D3 收尾新增)
7. 同步/异步术语一句话辨析(D3 收尾新增)

## 每周复盘存档

> 每周日完成 daily-schedule.md 文末「每周复盘 8 问」,链接归档于此。

(暂无)
